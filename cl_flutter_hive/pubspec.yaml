name: cl_flutter_hive
description: "Introducing an intuitive note-taking experience crafted in Flutter, our app seamlessly organizes your thoughts and tasks. Built with efficiency in mind, it leverages Hive, a lightweight and fast NoSQL database, ensuring swift data storage and retrieval. Capture your ideas effortlessly, categorize them with ease, and enjoy a fluid user experience—all in one place."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  shared_preferences: ^2.2.2
  provider: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.7

flutter:

  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  